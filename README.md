venv is used for python dependencies. To activate the virtual environment source the activate bin:

source data_compression/bin/activate

To run, execute the main function in the src directory:

python3 src/main.py
